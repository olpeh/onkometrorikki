{
  "name": "onkolansimetrorikki",
  "version": "1.0.0",
  "description": "Check if Länsimetro is broken at the moment",
  "scripts": {
    "start": "node ./backend/index.js",
    "dev": "elm-app start",
    "build": "elm-app build",
    "deploy:now": "cd ./build && now deploy --local-config=../now.json . && now alias --local-config=../now.json",
    "release:now": "npm run build && npm run deploy:now",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@koa/cors": "^2.2.1",
    "dotenv": "^6.0.0",
    "gtfs-realtime-bindings": "^0.0.4",
    "koa": "^2.5.1",
    "koa-bodyparser": "^4.2.1",
    "koa-request": "^1.0.0",
    "koa-route": "^3.2.0",
    "request": "^2.87.0",
    "twit": "^2.2.10"
  },
  "devDependencies": {
    "elm": "^0.18.0",
    "prettier": "1.13.0"
  }
}
